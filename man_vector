ФУНКЦИИ

(1)
t_vec			*ft_vnew(size_t size, size_t type_sz);
Создаёт новый вектор и выделяляет память для size кольчества элементов
с размером type_sz. Возвращает указатель на созданный вектор.
	Example:
	...
	t_vec *vector;

	vector = ft_vnew(5, sizeof(char*)); //выделит память для вектора из пяти строк
	...

(2)
void			ft_vpush_back(t_vec *v, void *val, size_t val_sz);
Добавляет элемент в конец вектора. Выделяет дополнительное количество памяти,
если надо.
	Example:
	...
	ft_vpush_back(vector, "Hello\n", sizeof(char*));
	...

(3)
void			ft_vpop_back(t_vec *v);
Удаляет элемент в конце вектора.

(4)
void			*ft_vat(t_vec *v, size_t at);
Возвращает ссылку на элемент в заданном положении в векторе.
	Example:
	...
	ft_vat(vector, 4); //вернёт четвёртый элемент вектора
	...

(5)
void			ft_vclear(t_vec *v);
Очищает элементы вектора.

(6)
void			ft_vdel(t_vec **v);
Удаляет вектор.
